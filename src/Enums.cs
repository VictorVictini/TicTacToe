namespace TicTacToe {
    public enum MoveState {
        Unused,
        Player,
        Bot
    };
    enum AIChance {
        Easy = 20,
        Medium = 50,
        Hard = 80,
        Impossible = 100
    };
}